import { LineEdit, ListView, Button, ScrollView, AboutSlint, VerticalBox } from "std-widgets.slint";

// ====== MODELOS ======
export struct PokemonRow { name: string }
export struct TypeTag { label: string, bg: brush, icon:  image}

export struct EvolutionNodeSlint {
    id: int,
    name: string,
    stage: int,
    row: int,
    method: string,      // "" para a base
    artwork: image,
    types: [TypeTag],
    dx: length,          // deslocamento horizontal opcional para encurtar cadeias lineares
}

export struct EvolutionEdgeSlint {  // opcional (para desenhar linhas)
    from: int,
    to: int,
    method: string,
}

export struct EvolutionLineSlint {
    x1: length,
    y1: length,
    x2: length,
    y2: length,
    method: string,
}

component EvolutionGraphView inherits Rectangle {
    in property <[EvolutionNodeSlint]> nodes;
    in property <[EvolutionEdgeSlint]> edges;      // pode deixar vazio se não for desenhar conexões
    in property <int> max-stage;
    in property <[EvolutionLineSlint]> lines;
    in property <int> max-rows;

    in property <bool> carregando;
    in-out property <int> selected_index;
    in property <int> select_pokemon_id;
    callback selected(id: int);
    callback apply-filter(value: string);

    // tamanhos de fonte
    property <length> name-size: 14px;
    property <length> method-size: 11px;

    // métricas
    // largura intermediária (desktop 156 era longa, 126 ficou curta demais)
    property <length> col-width: 140px; // 96 + 44 gap
    property <length> row-height: 220px; // node-h (200) + 20
    property <length> node-w: 90px;
    property <length> node-h: 200px; // altura maior para métodos longos
    property <length> total-width: (max-stage + 1) * col-width;
    property <length> total-height: max(max-rows,1) * row-height;
    height: total-height; // garante que o ScrollView considere a área completa

    // container absoluto
    content_layout := Rectangle {
        width: total-width;
        height: total-height;
        x: (parent.width - self.width)/2;
        y: 0px;
        background: transparent;
        // coloca cada node por stage/row
        for n in nodes : Rectangle {
            // top-left alinhado à grade; linhas usam centro = stage*col-width + node-w/2
            x: n.stage * col-width + n.dx; // aplica deslocamento para cadeias lineares
            y: n.row * row-height;
            width: node-w;
            height: node-h;
            if (true) : Rectangle {
                width: 90px; height: node-h;
                background: transparent;
                VerticalLayout {
                    alignment: LayoutAlignment.center;
                    spacing: 4px; // adiciona espaço entre imagem e ícones
                    HorizontalLayout {
                        alignment: center;
                        Image {
                            source: n.artwork; width: 90px; height:90px; 
                            TouchArea {
                                mouse-cursor: (!carregando && n.id != select_pokemon_id) ? pointer : default;
                                clicked => {
                                    if(!carregando && n.id != select_pokemon_id){
                                        apply-filter("");
                                        selected_index = n.id - 1;
                                        root.selected(n.id - 1);
                                    }
                                }
                            }
                        } 
                    }
                    // ícones tiny de tipos acima do nome com borda curva
                    if (n.types.length > 0) : HorizontalLayout {
                        spacing: 4px;
                        alignment: center;
                        for t in n.types : Rectangle {
                            width: 22px; height: 22px;
                            border-radius: 8px;
                            background: t.bg;
                            border-width: 1px;
                            border-color: #ffffff20;
                            // centraliza a imagem manualmente para evitar corte superior
                            Image { 
                                source: t.icon; 
                                width: 16px; height: 16px;
                                x: (parent.width - self.width)/2;
                                y: (parent.height - self.height)/2;
                            }
                        }
                    }
                    Text { text: n.name; color: #e5e7eb; font-size: name-size; horizontal-alignment: center; }
                    if (n.method != "") : Text { text: n.method; color: #9ca3af; font-size: method-size; horizontal-alignment: center; wrap: word-wrap; }
                }
            }
        }

    }

    // Overlay fora do layout para não deslocar colunas
    line_overlay := Rectangle {
        x: content_layout.x; y: content_layout.y;
        width: content_layout.width;
        height: content_layout.height;
        z: -1;
        background: transparent;
        // linhas
        for l in lines : Rectangle {
            // se linha vertical (x1==x2) desenha vertical
            if (abs(l.x2 - l.x1) < 1px) : Rectangle {
                x: l.x1 - 1px;
                y: min(l.y1, l.y2);
                width: 2px;
                height: abs(l.y2 - l.y1);
                background: #94a3b8;
            }
            // horizontal
            if (abs(l.x2 - l.x1) >= 1px) : Rectangle {
                x: min(l.x1, l.x2);
                y: l.y1 - 1px;
                width: abs(l.x2 - l.x1);
                height: 2px;
                background: #94a3b8;
            }
        }
    }

}

export struct PokemonDetail {
    name: string,
    id: int,
    height: float,
    weight: float,
    types: [TypeTag],
    hp: int,
    attack: int,
    defense: int,
    specialAttack: int,
    specialDefense: int,
    speed: int,
    artwork: image,
    total: int,
    ability1: string,
    ability2: string,
    hiddenAbility: string,
    error: string,
    color: brush,
    nodes: [EvolutionNodeSlint],
    edges: [EvolutionEdgeSlint],
    lines: [EvolutionLineSlint],
    max-stage: int,
    max-rows: int,
}

// ====== Componente lista de pokémon ======
component PokemonList inherits Rectangle {
    in property <[PokemonRow]> rows;
    in-out property <int> selected-index: -1;
    property <length> item-height: 40px;
    in property <bool> compact;
    in property <bool> carregando;

    callback selected(index: int);
    callback request-focus();
    request-focus() => { list-scope.focus(); }

    public function scroll_to_index(index: int) {
        if (index >= 0) {
            list.viewport-y = (((index + 1) * item-height) - (list.height / 2)) * (-1);
        }
    }

    background: transparent;
    border-radius: 6px;

    list-scope := FocusScope {
        width: parent.width;
        height: parent.height;

        // lista visual
        list := ScrollView  {
            width: parent.width;
            height: parent.height;
            mouse-drag-pan-enabled: true;
            VerticalBox {
                width: parent.width;
                spacing: 0px;
                for row[i] in rows: Rectangle {
                    height: item-height;
                    border-radius: 6px;
                    background: (i == selected_index)
                        ? #60a5fa
                        : ((Math.mod(i, 2) == 0) ? #10141c : #0b0f17);

                    Text {
                            text: row.name;
                            color: (i == selected_index) ? #202020 : #e5e7eb;
                            horizontal-stretch: 1;
                        }

                    TouchArea {
                        clicked => {
                            if(!carregando){
                                list-scope.focus();
                                selected_index = i;
                                root.selected(i);
                            }
                        }
                    }
                }
            }
        }

        // navegação por teclado + rolagem automática
        key-pressed(event) => {
            if(!compact && !carregando){
                if (event.text == Key.UpArrow) {
                    if (rows.length > 0) {
                        selected_index = Math.max(0, selected_index - 1);
                        root.selected(selected_index);
                        list.viewport-y = (((selected_index + 1) * item-height) - (list.height / 2)) * (-1);
                    }
                    accept
                }
                if (event.text == Key.DownArrow) {
                    if (rows.length > 0) {
                        if (selected_index < 0) { selected_index = 0; }
                        else { selected_index = Math.min(rows.length - 1, selected_index + 1); }
                        root.selected(selected_index);
                        list.viewport-y = (((selected_index + 1) * item-height) - (list.height / 2)) * (-1);
                    }
                    accept
                }
                if (event.text == Key.Home) {
                    if (rows.length > 0) {
                        selected_index = 0;
                        root.selected(selected_index);
                        list.viewport-y = (((selected_index + 1) * item-height) - (list.height / 2)) * (-1);
                    }
                    accept
                }
                if (event.text == Key.End) {
                    if (rows.length > 0) {
                        selected_index = rows.length - 1;
                        root.selected(selected_index);
                        list.viewport-y = (((selected_index + 1) * item-height) - (list.height / 2)) * (-1);
                    }
                    accept
                }
                reject
            } else {
                reject
            }
        }
    }
}

// ====== PAINEL DE DETALHES ======
component DetailPanel inherits Rectangle {
    in property <PokemonDetail> detail;
    in property <bool> carregando;
    in-out property <int> selected_index;
    callback selected(id: int);
    callback apply-filter(value: string);
    background: transparent;

    ScrollView {
        width: parent.width;
        height: parent.height;
        viewport-width: parent.width;
        mouse-drag-pan-enabled: true;

        VerticalBox {
            width: parent.width;
            alignment: LayoutAlignment.start;
            spacing: 10px;

            // sprite centralizada
            Rectangle {
                width: parent.width;
                height: 260px;
                Rectangle {
                    width: 170px; height: 170px;
                    x: (parent.width - self.width) / 2;
                    y: (parent.height - self.height) / 2;
                    border-radius: 10px; 
                    background: @radial-gradient(circle,
                        detail.color 0%,
                        detail.color 20%, 
                        detail.color 20%,
                        detail.color 30%,
                        transparent 75%,
                        transparent 100%);
                    Image {
                        source: root.detail.artwork;
                        width: parent.width;
                        height: parent.height;
                    }
                }
            }

            // chips de tipos + nome + infos
            VerticalLayout {
                visible: true;
                alignment: LayoutAlignment.start;
                spacing: 15px;

                // chips (centralizados)
                HorizontalLayout {
                    width: parent.width;
                    Rectangle { horizontal-stretch: 1; }

                    HorizontalLayout {
                        spacing: 6px;
                        for t in root.detail.types: Rectangle {
                            height: 20px;
                            border-radius: 8px;
                            background: t.bg;

                            HorizontalLayout {
                                padding-left: 8px;
                                padding-right: 8px;
                                Image {
                                    source: t.icon;
                                    y:parent.y + ((parent.height - self.height) / 2);
                                    width: 18px;
                                    height: 18px;
                                }
                                Text {
                                    text: t.label;
                                    color: #ffffff;
                                    font-size: 14px;
                                    vertical-alignment: center;
                                    font-weight: 700;
                                }
                            }
                        }
                    }

                    Rectangle { horizontal-stretch: 1; }
                }

                Text {
                    text: root.detail.name == "" ? "Selecione um Pokémon" : root.detail.name;
                    font-size: 24px; color: #f9fafb;
                    font-weight: 700;
                    width: parent.width; horizontal-alignment: left;
                }

                // infos básicas
                HorizontalLayout {
                    Text { text: "ID: " + (root.detail.id == 0 ? "-" : root.detail.id); color: #e5e7eb;  font-size: 14px; }
                    Text { text: "Altura: " + (root.detail.height == 0.0 ? "-" : root.detail.height + " m"); color: #e5e7eb;  font-size: 14px; }
                    Text { text: "Peso: " + (root.detail.weight == 0.0 ? "-" : root.detail.weight + " kg"); color: #e5e7eb;  font-size: 14px; }
                }

                HorizontalLayout {
                    Text { text: "Habilidade: " + (root.detail.ability1 == "" ? "-" : root.detail.ability1); color: #e5e7eb;  font-size: 14px; }
                    Text { text: "Habilidade 2: " + (root.detail.ability2 == "" ? "-" : root.detail.ability2); color: #e5e7eb;  font-size: 14px; }
                }
                HorizontalLayout {
                    Text { text: "Hidden: " + (root.detail.hiddenAbility == "" ? "-" : root.detail.hiddenAbility); color: #e5e7eb;  font-size: 14px; }
                }
            }

            if (root.detail.error != "") : Text { text: root.detail.error; color: #ef4444; }

            // barras
            VerticalLayout {
                padding-top: 10px;
                spacing: 10px;

                // vida
                HorizontalLayout {
                    spacing: 8px;
                    Text { text: "Vida"; width: 120px; color: #9ca3af; }
                    Rectangle { // trilho
                        height: 12px; border-radius: 6px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle { // preenchimento
                            height: 12px; border-radius: 6px; x: 0px;
                            // limita entre 0 e 1
                            width: parent.width * min((root.detail.hp / 255.0), 1.0);
                            background: rgb(105, 220, 18);
                        }
                    }
                    Text { text: root.detail.hp; width: 44px; color: #e5e7eb; }
                }

                // ataque
                HorizontalLayout {
                    spacing: 8px;
                    Text { text: "Ataque"; width: 120px; color: #9ca3af; }
                    Rectangle { // trilho
                        height: 12px; border-radius: 6px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle { // preenchimento
                            height: 12px; border-radius: 6px; x: 0px;
                            // limita entre 0 e 1
                            width: parent.width * min((root.detail.attack / 255.0), 1.0);
                            background: rgb(239, 204, 24);
                        }
                    }
                    Text { text: root.detail.attack; width: 44px; color: #e5e7eb; }
                }
                // defesa
                HorizontalLayout {
                    spacing: 8px;
                    Text { text: "Defesa"; width: 120px; color: #9ca3af; }
                    Rectangle { // trilho
                        height: 12px; border-radius: 6px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle { // preenchimento
                            height: 12px; border-radius: 6px; x: 0px;
                            // limita entre 0 e 1
                            width: parent.width * min((root.detail.defense / 255.0), 1.0);
                            background: rgb(232, 100, 18);
                        }
                    }
                    Text { text: root.detail.defense; width: 44px; color: #e5e7eb; }
                }
                // ataque especial
                HorizontalLayout {
                    spacing: 8px;
                    Text { text: "Ataque Especial"; width: 120px; color: #9ca3af; }
                    Rectangle { // trilho
                        height: 12px; border-radius: 6px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle { // preenchimento
                            height: 12px; border-radius: 6px; x: 0px;
                            // limita entre 0 e 1
                            width: parent.width * min((root.detail.specialAttack / 255.0), 1.0);
                            background: rgb(20, 195, 241);
                        }
                    }
                    Text { text: root.detail.specialAttack; width: 44px; color: #e5e7eb; }
                }
                // defesa especial
                HorizontalLayout {
                    spacing: 8px;
                    Text { text: "Defesa Especial"; width: 120px; color: #9ca3af; }
                    Rectangle { // trilho
                        height: 12px; border-radius: 6px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle { // preenchimento
                            height: 12px; border-radius: 6px; x: 0px;
                            // limita entre 0 e 1
                            width: parent.width * min((root.detail.specialDefense / 255.0), 1.0);
                            background: rgb(74, 106, 223);
                        }
                    }
                    Text { text: root.detail.specialDefense; width: 44px; color: #e5e7eb; }
                }
                // velocidade
                HorizontalLayout {
                    spacing: 8px;
                    Text { text: "Velocidade"; width: 120px; color: #9ca3af; }
                    Rectangle { // trilho
                        height: 12px; border-radius: 6px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle { // preenchimento
                            height: 12px; border-radius: 6px; x: 0px;
                            // limita entre 0 e 1
                            width: parent.width * min((root.detail.speed / 255.0), 1.0);
                            background: rgb(239, 99, 200);
                        }
                    }
                    Text { text: root.detail.speed; width: 44px; color: #e5e7eb; }
                }

                // total
                HorizontalLayout { spacing: 8px;
                    Text { text: "Total"; width: 120px; color: #d1d5db; }
                    Rectangle {
                        height: 14px; border-radius: 7px; background: #1f2937; horizontal-stretch: 1;
                        Rectangle {
                            height: 14px; border-radius: 7px; x: 0px;
                            width: parent.width * min((root.detail.total / 1200.0), 1.0);
                            background: #ffffff;
                        }
                    }
                    Text { text: root.detail.total; width: 44px; color: #fef3c7; }
                }
                // evoluções
                EvolutionGraphView {
                    nodes: root.detail.nodes;
                    edges: root.detail.edges;
                    max-stage: root.detail.max-stage;
                    lines: root.detail.lines; // adiciona binding das linhas
                    max-rows: root.detail.max-rows;
                    carregando: carregando;
                    selected_index: selected_index;
                    select_pokemon_id: detail.id;
                    selected(id) => { root.selected(id);}
                    apply-filter(value) => { root.apply-filter(value);}
                }
            }
        }
    }
}

component Splash inherits Rectangle {
    in property <string> app-version;

    height: 100%;
    width: 100%;
    background: #0B0F17;
    HorizontalLayout {
        height: 100%;
        width: 100%;
        alignment: LayoutAlignment.center;
        VerticalLayout {
            spacing: 10px;
            alignment: LayoutAlignment.center;
            height: 50%;
            width: 50%;

            Text { 
                text: "Slindex";
                font-size: 24px;
                horizontal-alignment: TextHorizontalAlignment.center;
            }
            Text { text: root.app-version;
                font-size: 15px;
                horizontal-alignment: TextHorizontalAlignment.center;
            }
            Image {
                source: @image-url("app.png");
                width: 50%;
                height: 50%;
            }
            AboutSlint {
                height: 20%;
                width: 20%;
            }
            Text { 
                text: "Dados fornecidos por PokéAPI";
                font-size: 15px;
                horizontal-alignment: TextHorizontalAlignment.center;
            }
            Text { 
                text: "Pokémon and Pokémon character names are trademarks of Nintendo.";
                font-size: 15px;
                horizontal-alignment: TextHorizontalAlignment.center;
                wrap: word-wrap;
            }
        }
    }
}

// ====== APP RESPONSIVO ======
export component App inherits Window {
    title: "SlinDex";
    // tamanhos mínimos pra mobile
    min-width: 320px; min-height: 480px;
    icon: @image-url("app.png");

    preferred-width: 900px;
    preferred-height: 700px;

    // breakpoint simples
    property <bool> compact: self.width < 720px;

    in-out property <string> filter;
    in-out property <int> selected_index; // mantém seleção na lista
    in-out property <bool> visualiza_pokemon: false;
    in-out property <bool> carregando: false;
    in property <[PokemonRow]> rows;
    in property <PokemonDetail> detail;
    in property <string> app-version;

    in-out property <bool> splash: true;

    callback request-load();
    callback select(index: int);
    callback apply-filter(text: string);

    callback back-to-list();
    back-to-list() => {
        if(!carregando){
            root.visualiza_pokemon = false;
        }
    }

    if(splash):Splash
    {
        app-version: root.app-version;
    }

    if(!splash):VerticalLayout {
        spacing: 12px; padding: 12px;

        // --- top bar ---
        Rectangle {
            // altura fixa evita que o layout-info da barra dependa dos filhos
            height: 60px;
            horizontal-stretch: 1;

            // Versão normal (busca + botões)
            if (!(root.compact && root.visualiza_pokemon)) : HorizontalLayout {
                x: 0px; y: 0px; width: parent.width; height: parent.height;
                spacing: 8px;

                LineEdit {
                    placeholder-text: "Buscar Pokémon";
                    text <=> root.filter;
                    horizontal-stretch: 1;
                    accepted(text) => { root.apply-filter(text); }
                }
                Button { text: "Filtrar"; clicked => { root.apply-filter(root.filter); } }
            }

            // Versão compacta com "Voltar"
            if (root.compact && root.visualiza_pokemon) : HorizontalLayout {
                x: 0px; y: 0px; width: parent.width; height: parent.height;
                spacing: 8px;
                Button {
                    text: "← Voltar";
                    clicked => { root.back-to-list(); }
                }
            }
        }

        // --- conteúdo ---
        Rectangle {
            horizontal-stretch: 1;
            vertical-stretch: 1;

            // LISTA
            list_pane := PokemonList {
                // geometria muda com o breakpoint
                x: 0px;
                y: 0px;
                width: root.compact ? parent.width : 250px;
                height: parent.height;

                rows: root.rows;
                selected-index <=> root.selected_index;
                compact: compact;

                carregando: carregando;

                // no compacto, ao selecionar já navega para o detalhe
                selected(i) => {
                    root.select(i);
                    if (root.compact) { root.visualiza_pokemon = true; }
                }

                // visibilidade: no compacto alterna com o detalhe; no amplo fica sempre visível
                visible: root.compact ? !root.visualiza_pokemon : true;
            }

            // DETALHE
            detail_pane := DetailPanel {
                y: 0px;
                x: root.compact ? 0px : (list_pane.width + 12px);
                width: root.compact ? parent.width : (parent.width - (list_pane.width + 12px));
                height: parent.height;
                detail: root.detail;

                // visibilidade: no compacto alterna com a lista; no amplo fica sempre visível
                visible: root.compact ? root.visualiza_pokemon : true;

                carregando: carregando;
                selected_index: selected_index;
                selected(id) => { 
                    root.select(id);
                    list_pane.scroll_to_index(id);
                }
                apply-filter(value) => { root.apply-filter(value);}
            }
        }
    }
}